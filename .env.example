# Copy to .env only if your local launcher loads dotenv files.
# Values below are non-functional placeholders for an authorized local environment.
APP_DATA_FILE=./data/synthetic_safety_events.csv
APP_DISMISSED_DRIVERS_FILE=./data/synthetic_dismissed_drivers.csv
APP_AUTH_USERS_FILE=./auth_users.json
APP_ALLOW_DEMO_USERS=false
APP_PREDICTOR_MODE=mock
APP_SECRET_KEY=replace-with-a-random-secret-of-at-least-32-characters
APP_TOKEN_TTL_SECONDS=28800
APP_CORS_ORIGINS=http://localhost:8080
FORECAST_DAYS=45
RECENT_HISTORY_DAYS=14
